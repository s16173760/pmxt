[pytest]
testpaths = tests
python_files = test_*.py
norecursedirs = generated
asyncio_mode = auto
markers =
    integration: requires a running PMXT sidecar server (deselected by default)
addopts = -m "not integration"
